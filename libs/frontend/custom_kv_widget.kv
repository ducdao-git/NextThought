#: import icon kivysome.icon

<DividerLabel@Label>
    size_hint: 1, None
    height: dp(1.5)

    canvas:
        Color:
            rgba: app.theme_palette['divider_color']
        Rectangle:
            pos: self.pos
            size: self.size


<IconButton>
    size_hint: 1, 1
    background_down: ''
    background_normal: ''
    background_color: [1, 1, 0, 0]
    text_size: self.size
    halign: 'center'
    valign: 'center'
    markup: True
    font_size: sp(20)
    color: app.theme_palette['support_text_color']


<RouteTitleLabel@Label>
    size_hint: 7, 1
    text_size: self.size
    halign: 'left'
    valign: 'center'
    font_size: sp(20)
    color: app.theme_palette['primary_color']


<ResizableTextInput@TextInput>:
    size_hint: 8, None
    height: self.minimum_height
    background_color: [1, 0, 0, 0]
    cursor_color: app.theme_palette['primary_color']
    padding: [dp(8), dp(8)]
    write_tab: False
    font_size: sp(14)
    multiline: True
    foreground_color: app.theme_palette['text_background_color']


#<ResizableNumberInput>


<InputLayout@BoxLayout>:
    orientation: 'horizontal'
    size_hint: 1, None
    height: self.minimum_height

    canvas.before:
        Color:
            rgba: app.theme_palette['input_background_color']
        RoundedRectangle:
            pos: self.pos
            size: self.size


<RoundedButton@Button>
    size_hint: 1, None
    height: self.texture_size[1]
    padding: dp(8), dp(8)
    background_normal: ''
    background_down: ''
    background_color: (0, 0, 0, 0)
    bg_color: (0, 0, 0, 0)
    markup: True

    opacity: 0.95
    canvas.before:
        Color:
            rgba: self.bg_color
        RoundedRectangle:
            pos: self.pos
            size: self.size

<InfoLabel@Label>
    size_hint: 1, None
    height: self.texture_size[1]
    text_size: self.width, None
    padding: dp(12), dp(2)
    line_height: 1.2
    color: app.theme_palette['text_secondary_color']
#    bg_color: (0, 0, 0, 0)

    opacity: 0.8
    canvas.before:
        Color:
            rgba: app.theme_palette['secondary_color']
        RoundedRectangle:
            pos: self.pos
            size: self.size

